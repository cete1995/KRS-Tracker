"use client";import {useState} from "react";import {courses as seed} from "@/data/courses";import {PageTitle} from "@/components/ui";
export default function CurriculumAdmin(){const [courses,setCourses]=useState(seed),[saved,setSaved]=useState(false);return <><PageTitle title="Kelola Kurikulum" subtitle="Tentukan semester, kewajiban, dan posisi awal pada curriculum map." actions={<button onClick={()=>setSaved(true)} className="btn btn-primary">{saved?"Perubahan tersimpan":"Simpan perubahan"}</button>}/><div className="card overflow-hidden"><div className="table-wrap"><table className="table"><thead><tr><th>Mata kuliah</th><th>Semester</th><th>Sifat</th><th>Posisi X</th><th>Posisi Y</th></tr></thead><tbody>{courses.map((c,i)=><tr key={c.id}><td><div className="font-bold">{c.name}</div><div className="text-xs text-slate-500">{c.code}</div></td><td><select className="field w-24" value={c.semester} onChange={e=>{setSaved(false);setCourses(x=>x.map((v,j)=>j===i?{...v,semester:+e.target.value}:v))}}>{[1,2,3,4,5,6,7,8].map(n=><option key={n}>{n}</option>)}</select></td><td><select className="field"><option>Wajib</option><option>Pilihan</option></select></td><td><input className="field w-24" type="number" defaultValue={(c.semester-1)*270}/></td><td><input className="field w-24" type="number" defaultValue={(i%8)*125}/></td></tr>)}</tbody></table></div></div></>}
