import type {Course,CourseStatus} from "@/lib/types";import {categoryConfig,statusConfig} from "@/lib/config";
export function PageTitle({title,subtitle,actions}:{title:string;subtitle:string;actions?:React.ReactNode}){return <div className="mb-6 flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-2xl font-bold tracking-tight text-ink md:text-[28px]">{title}</h1><p className="mt-1 text-sm text-slate-500">{subtitle}</p></div>{actions}</div>}
export function StatusBadge({status}:{status:CourseStatus}){const s=statusConfig[status];return <span className="badge" style={{background:s.color+"16",color:s.color}}><span className="mr-1.5 h-1.5 w-1.5 rounded-full" style={{background:s.color}}/>{s.label}</span>}
export function CategoryBadge({category}:{category:Course["category"]}){const c=categoryConfig[category];return <span className="badge" style={{background:c.soft,color:c.color}}>{c.label}</span>}
export function CourseCard({course,action,onClick}:{course:Course;action?:React.ReactNode;onClick?:()=>void}){const cat=categoryConfig[course.category];return <div onClick={onClick} className="rounded-xl border border-line bg-white p-4 transition hover:-translate-y-0.5 hover:shadow-card" style={{borderLeft:`4px solid ${cat.color}`}}><div className="flex items-start justify-between gap-2"><div><div className="text-[11px] font-bold tracking-wide text-slate-500">{course.code}</div><div className="mt-1 text-sm font-bold">{course.name}</div></div>{action}</div><div className="mt-3 flex items-center justify-between"><span className="text-xs text-slate-500">{course.credits} SKS · Semester {course.semester}</span><StatusBadge status={course.status}/></div></div>}
